// Fired by BottomNav when the Reels tab is tapped while already on /reels — Instagram-style
// "tap the active tab to refresh and jump back to the top" behavior.
export const REELS_REFRESH_EVENT = 'zomafeeds-reels-refresh'
